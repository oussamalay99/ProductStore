# Set Up Guide

## Backend

- Configure `.env` file with your own variables
- Push the schemas established by drizzle to your database: `npm run db:push`
- Run the expressJs app in dev mode: `npm run dev`

## Frontend

- Configure `.env` file with your own variables
- Run the Vite app in dev mode: `npm run dev`